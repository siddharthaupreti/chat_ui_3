import 'package:flutter/material.dart';

class InformationWindow extends StatelessWidget {
  const InformationWindow({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("This is sid"),
    );
  }
}
